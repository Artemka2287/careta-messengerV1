package com.careta.server;

// Статусы доставки и прочтения сообщений
public enum MessageStatus {
    SENT,      // Отправлено (сохранено в БД, но получатель не в сети)
    DELIVERED, // Доставлено (получатель онлайн и получил пакет)
    READ       // Прочитано (получатель открыл историю чата)
}